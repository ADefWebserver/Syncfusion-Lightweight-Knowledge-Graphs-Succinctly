Task:
Create a Markdown file and save it in the docs/ directory. Actually create the file
The file should contain a detailed, structured plan for implementing the following features
(I will list them after this instruction).
Requirements:
Use clear section headings.
Include Mermaid diagrams to illustrate both system structure and process flows.
Do not put \n inside Mermaid node labels because VS Code's Mermaid renderer doesn't support that.
Avoid edge labels like "1. User clicks AI"
because Mermaid's renderer interprets 1. as a markdown numbered list inside the label.
Provide enough detail that a developer could begin implementing the features
based on this document alone.
Use Markdown formatting throughout.
Do not include any file‑system commands; simply output the Markdown content.
Output Format:
Return only the contents of the .md file, starting with a top‑level # heading.
Features to document:

The next feature is an interactive knowledge graph explorer implemented in
Components/Pages/Graph.razor. By this point, the file-based graph is already loaded
into a singleton GraphStore, and HelpDeskGraphBuilder and GraphStore are registered in
dependency injection. The graph contains Ticket, TicketDetail, Requester, Status, and
Day nodes connected by REQUESTED_BY, HAS_DETAIL, HAS_STATUS, and OCCURRED_ON edges.

Design a Blazor Server page at /graph that renders this graph in the browser with
Cytoscape.js through JavaScript interop. A user should be able to filter node types,
select a node to inspect its properties and neighbors, toggle full-screen mode, and
rebuild the graph from the current database without reloading the page.

Put the browser-side integration in wwwroot/js/graph-renderer.js. Cytoscape.js should
be available as wwwroot/js/cytoscape.min.js and referenced by the application host.
Give each node type a recognizable style: Ticket is a blue round-rectangle,
TicketDetail a green ellipse, Requester a red ellipse, Status an orange diamond, and
Day a teal rectangle. Edges need visible labels, arrowheads, and bezier curves, and
selected elements need a distinct :selected style. Use Cytoscape's animated "cose"
force-directed layout.

The renderer should export four functions for Blazor to call:
initCytoscape(containerId, graphData, dotNetRef), updateGraph(containerId, graphData),
destroyCytoscape(containerId), and toggleFullScreen(containerId). During
initialization, register a node tap handler that invokes OnNodeClicked on the .NET
reference. Tapping the background should clear the current selection.

Components/Pages/Graph.razor should use @page "/graph", @rendermode
InteractiveServer, and @implements IAsyncDisposable, and inject IJSRuntime,
GraphStore, and HelpDeskGraphBuilder. Its toolbar should include a Rebuild Graph
button that is disabled while work is in progress, badges for node and edge counts,
the graph's generated timestamp, and a Full Screen toggle.

Build the entity-type checkboxes from GraphStore.NodeTypes. Initially show the
connected core of Ticket, Requester, and Status, while allowing TicketDetail and Day
to be enabled when needed. If none of those default core types exist, show every
available type instead. The graph itself belongs in a viewport-sized div with the ID
cy. When a node is selected, show a detail panel containing its label, type, every
Data property, and its connected nodes along with each relationship label.

Document the page's data shaping and interop lifecycle in enough detail to implement
it. BuildGraphData should include only visible node types and should include an edge
only when both of its endpoints are visible. Return the shape expected by the
renderer: { nodes:[{id,label,type}], edges:[{id,source,target,label}] }.

On the first OnAfterRenderAsync call, create a DotNetObjectReference and pass it to
initCytoscape. Mark OnNodeClicked(nodeId) as JSInvokable; it should retrieve the node
from GraphStore, populate the detail panel, and collect neighbors from both OutEdges
and InEdges. ToggleType should update the visible set, close the detail panel, and
call updateGraph rather than initializing Cytoscape again. RebuildAsync should call
HelpDeskGraphBuilder.BuildAsync, then GraphStore.ReloadAsync, refresh the available
types, and reinitialize the diagram. DisposeAsync should destroy the Cytoscape
instance and dispose the DotNetObjectReference, ignoring JSDisconnectedException if
the Blazor circuit has already ended.

Finally, include the required Program.cs configuration. Large graphs can create
large interop messages, so raise HubOptions.MaximumReceiveMessageSize through
AddInteractiveServerComponents to prevent SignalR from dropping the circuit.